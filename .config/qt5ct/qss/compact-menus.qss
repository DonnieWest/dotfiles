/* Compact menu styling - reduces padding and margins */

QMenu {
    padding: 2px;
}

QMenu::item {
    padding: 4px 20px 4px 8px;
    margin: 0px;
}

QMenu::separator {
    height: 1px;
    margin: 2px 0px;
}

QMenuBar {
    padding: 2px;
}

QMenuBar::item {
    padding: 4px 8px;
    margin: 0px;
}

/* Compact combo boxes */
QComboBox {
    padding: 2px 6px;
}

QComboBox QAbstractItemView {
    padding: 2px;
}

QComboBox QAbstractItemView::item {
    padding: 4px 8px;
    min-height: 18px;
}

/* Compact list views */
QListView::item {
    padding: 2px 4px;
    min-height: 18px;
}

/* Compact tree views */
QTreeView::item {
    padding: 2px 4px;
    min-height: 18px;
}

/* Compact buttons */
QPushButton {
    padding: 4px 12px;
}

/* Compact tool buttons */
QToolButton {
    padding: 3px;
}
